<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todo_dawidkaminski">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".DetailsActivity"
            android:label="@string/DetailsHeading"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="com.example.todo_dawidkaminski.MainActivity"
                android:value="com.example.todo_dawidkaminski"/>
        </activity>
        <activity
            android:name=".NewItemActivity"
            android:label="@string/AddNewHeading"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="com.example.todo_dawidkaminski.MainActivity"
                android:value="com.example.todo_dawidkaminski" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/HomepageHeading"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>